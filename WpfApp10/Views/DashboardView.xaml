<UserControl x:Class="WpfApp10.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ================= ЗАГОЛОВОК ================= -->
        <TextBlock Text="Главная"
                   FontSize="28"
                   FontWeight="Bold"
                   Margin="0 0 0 15"/>

        <!-- ================= ПОИСК ================= -->
        <StackPanel Grid.Row="1" Margin="0 0 0 20">

            <!-- SEARCH BOX -->
            <Grid>
                <TextBox Height="42"
                         FontSize="16"
                         Padding="14 6"
                         VerticalContentAlignment="Center"
                         Text="{Binding SearchText,
                                UpdateSourceTrigger=PropertyChanged}" />

                <!-- PLACEHOLDER -->
                <TextBlock Text="Поиск по задачам, проектам, документам..."
                           Margin="18 0 0 0"
                           VerticalAlignment="Center"
                           Foreground="Gray"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SearchText}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- SEARCH RESULTS -->
            <Border Margin="0 6 0 0"
                    BorderBrush="#DDD"
                    BorderThickness="1"
                    CornerRadius="6"
                    Visibility="{Binding HasSearchResults,
                        Converter={StaticResource BoolToVisibility}}">

                <ListView ItemsSource="{Binding SearchResults}"
                          SelectedItem="{Binding SelectedSearchResult}"
                          Height="160"
                          BorderThickness="0">

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="6"
                                    Padding="12"
                                    CornerRadius="6"
                                    BorderBrush="#DDD"
                                    BorderThickness="1"
                                    Background="White">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="16"
                                               FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Type}"
                                               FontSize="12"
                                               Opacity="0.6"
                                               Margin="0 4 0 0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>
            </Border>

        </StackPanel>

        <!-- ================= ОСНОВНОЙ КОНТЕНТ ================= -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- ===== СЧЁТЧИКИ ===== -->
            <WrapPanel Margin="0 0 20 0">

                <Border Padding="20"
                        Background="#F5F5F5"
                        Margin="0 0 12 12"
                        CornerRadius="6">
                    <TextBlock Text="{Binding ProjectsCount,
                        StringFormat=Проектов: {0}}"
                               FontSize="20"/>
                </Border>

                <Border Padding="20"
                        Background="#F5F5F5"
                        Margin="0 0 12 12"
                        CornerRadius="6">
                    <TextBlock Text="{Binding TasksCount,
                        StringFormat=Задач: {0}}"
                               FontSize="20"/>
                </Border>

                <Border Padding="20"
                        Background="#F5F5F5"
                        Margin="0 0 12 12"
                        CornerRadius="6">
                    <TextBlock Text="{Binding DocumentsCount,
                        StringFormat=Документов: {0}}"
                               FontSize="20"/>
                </Border>

            </WrapPanel>

            <!-- ===== ДИАГРАММЫ ===== -->
            <StackPanel Grid.Column="1">

                <TextBlock Text="Задачи по статусам"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0 0 0 10"/>

                <lvc:PieChart Series="{Binding TasksByStatusSeries}"
                              Height="220"
                              LegendLocation="Right"/>

                <TextBlock Text="Проекты по владельцам"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0 20 0 10"/>

                <lvc:CartesianChart Series="{Binding ProjectsByOwnerSeries}"
                                    Height="220">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding OwnersLabels}"/>
                    </lvc:CartesianChart.AxisX>
                </lvc:CartesianChart>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
