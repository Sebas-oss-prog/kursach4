<UserControl x:Class="WpfApp10.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="RoundIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Width="{TemplateBinding Width}"
                                Height="{TemplateBinding Height}"
                                CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0 0 0 12">
                <Button Content="&lt;"
                        Style="{StaticResource RoundIconButtonStyle}"
                        Command="{Binding PrevMonthCommand}"/>

                <TextBlock Text="{Binding MonthTitle}"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="14 0"/>

                <Button Content="&gt;"
                        Style="{StaticResource RoundIconButtonStyle}"
                        Command="{Binding NextMonthCommand}"/>
            </StackPanel>

            <!-- CALENDAR -->
            <ItemsControl Grid.Row="1"
                          ItemsSource="{Binding Days}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="7"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="4"
                                Padding="6"
                                CornerRadius="8"
                                Background="White"
                                BorderBrush="#DDD"
                                BorderThickness="1"
                                MouseLeftButtonDown="Day_Click">
                            <StackPanel>

                                <TextBlock Text="{Binding Day}"
                                           FontWeight="Bold"
                                           Margin="0 0 0 4"/>

                                <!-- TASKS -->
                                <ItemsControl ItemsSource="{Binding Tasks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="11"
                                                       Foreground="#1976D2"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxWidth="130"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                                <!-- PROJECTS -->
                                <ItemsControl ItemsSource="{Binding Projects}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Title}"
                                                       FontSize="11"
                                                       Foreground="#7B1FA2"
                                                       TextTrimming="CharacterEllipsis"
                                                       MaxWidth="130"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>

                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <!-- SIDE PANEL -->
        <Border Width="360"
                Background="#F4F4F4"
                Padding="20"
                HorizontalAlignment="Right"
                Panel.ZIndex="10">

            <Border.RenderTransform>
                <TranslateTransform X="380"/>
            </Border.RenderTransform>

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Setter Property="Opacity" Value="0"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsPanelVisible}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.25"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.X)"
                                                         To="0"
                                                         Duration="0:0:0.25"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <StackPanel>

                <Button Content="✕"
                        Style="{StaticResource RoundIconButtonStyle}"
                        HorizontalAlignment="Right"
                        Command="{Binding ClosePanelCommand}"/>

                <TextBlock Text="{Binding SelectedDayTitle}"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0 10"/>

                <!-- TASKS -->
                <TextBlock Text="Задачи:" FontWeight="Bold"/>
                <ItemsControl ItemsSource="{Binding SelectedDay.Tasks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0 4">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                <ItemsControl ItemsSource="{Binding Employees}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FullName}"
                                                       FontSize="12"
                                                       Margin="10 0 0 0"
                                                       Opacity="0.8"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- PROJECTS -->
                <TextBlock Text="Проекты:" FontWeight="Bold" Margin="0 10 0 0"/>
                <ItemsControl ItemsSource="{Binding SelectedDay.Projects}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0 4">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding Owner}"
                                           FontStyle="Italic"
                                           FontSize="12"
                                           Opacity="0.7"/>
                                <ItemsControl ItemsSource="{Binding Employees}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding FullName}"
                                                       FontSize="12"
                                                       Margin="10 0 0 0"
                                                       Opacity="0.8"/>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
