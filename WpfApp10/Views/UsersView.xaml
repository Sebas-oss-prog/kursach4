<UserControl x:Class="WpfApp10.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:helpers="clr-namespace:WpfApp10.Helpers"
             xmlns:viewmodels="clr-namespace:WpfApp10.ViewModels">

    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <helpers:NullToBoolConverter x:Key="NullToBool"/>
        <helpers:BooleanNegationConverter x:Key="BooleanNegationConverter"/>

        <Style x:Key="ListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0 2"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для полупрозрачного фона -->
        <Style x:Key="OverlayBackgroundStyle" TargetType="Border">
            <Setter Property="Background" Value="#80000000"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsEditorVisible}" Value="True">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- ===== СТИЛЬ КНОПОК КАК В БОКОВОЙ ПАНЕЛИ ===== -->
        <Style x:Key="SidePanelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12 8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Left"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для кнопки Сохранить -->
        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14 8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#45A049"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3D8B40"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для кнопки Отмена -->
        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14 8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#D0D0D0"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для кнопки закрытия -->
        <Style x:Key="CloseXButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#666"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Width="28"
                                Height="28"
                                CornerRadius="14"
                                Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0F0F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <!-- Основной контент - список пользователей -->
        <DockPanel>
            <!-- Заголовок и кнопки -->
            <StackPanel DockPanel.Dock="Top" Margin="0 0 0 10">
                <TextBlock Text="Управление пользователями"
                           FontSize="22"
                           FontWeight="Bold"
                           Margin="0 0 0 10"/>

                <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                    <Button Content="Добавить"
                            Width="100"
                            Style="{StaticResource SidePanelButtonStyle}"
                            Command="{Binding AddCommand}"/>

                    <Button Content="Редактировать"
                            Width="120"
                            Margin="10 0 0 0"
                            Style="{StaticResource SidePanelButtonStyle}"
                            Command="{Binding EditCommand}"
                            IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBool}}"/>

                    <Button Content="Удалить"
                            Width="100"
                            Margin="10 0 0 0"
                            Style="{StaticResource SidePanelButtonStyle}"
                            Command="{Binding DeleteCommand}"
                            IsEnabled="{Binding SelectedUser, Converter={StaticResource NullToBool}}"/>
                </StackPanel>
            </StackPanel>

            <!-- Список пользователей (растянут на всю ширину) -->
            <ListView ItemsSource="{Binding Users}"
                      SelectedItem="{Binding SelectedUser}"
                      ItemContainerStyle="{StaticResource ListViewItemStyle}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="50"/>
                        <GridViewColumn Header="Логин" DisplayMemberBinding="{Binding Login}" Width="150"/>
                        <GridViewColumn Header="ФИО" DisplayMemberBinding="{Binding FullName}" Width="250"/>
                        <GridViewColumn Header="Роль" DisplayMemberBinding="{Binding Role}" Width="100"/>
                        <GridViewColumn Header="Пароль" DisplayMemberBinding="{Binding Password}" Width="150"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>

        <!-- Полупрозрачный фон поверх списка (только когда открыта панель) -->
        <Border Style="{StaticResource OverlayBackgroundStyle}"
                Panel.ZIndex="10">
            <Border.Resources>
                <Style TargetType="Border">
                    <Setter Property="Opacity" Value="0"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditorVisible}" Value="True">
                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1"
                                                         Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Resources>
        </Border>

        <!-- Панель редактирования (поверх всего) -->
        <Border Width="400"
                Padding="25"
                Background="White"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Panel.ZIndex="20"
                BorderBrush="#E0E0E0"
                BorderThickness="1">

            <Border.RenderTransform>
                <TranslateTransform Y="-50"/>
            </Border.RenderTransform>

            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Opacity" Value="0"/>
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform Y="-50"/>
                        </Setter.Value>
                    </Setter>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditorVisible}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>

                            <DataTrigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="0" To="1"
                                                         Duration="0:0:0.3"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)"
                                                         From="-30" To="-50"
                                                         Duration="0:0:0.3"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>

                            <DataTrigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.2"/>
                                        <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.Y)"
                                                         To="-30"
                                                         Duration="0:0:0.2"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Заголовок и кнопка закрытия -->
                <DockPanel Grid.Row="0" Margin="0 0 0 20">
                    <TextBlock Text="{Binding PanelTitle}"
                               FontSize="20"
                               FontWeight="Bold"
                               DockPanel.Dock="Left"/>

                    <Button Content="✕"
                            Style="{StaticResource CloseXButtonStyle}"
                            DockPanel.Dock="Right"
                            HorizontalAlignment="Right"
                            Command="{Binding CloseCommand}"/>
                </DockPanel>

                <!-- Форма -->
                <StackPanel Grid.Row="1" Margin="0 0 0 20">
                    <TextBlock Text="Логин" FontWeight="SemiBold" Margin="0 0 0 5"/>
                    <TextBox Text="{Binding SelectedUser.Login}" 
                             Height="35"
                             Padding="10"
                             IsEnabled="{Binding IsEditMode, Converter={StaticResource BooleanNegationConverter}}"/>

                    <TextBlock Text="Пароль" FontWeight="SemiBold" Margin="0 10 0 5"/>
                    <PasswordBox x:Name="PasswordBox" 
                                 Height="35"
                                 Padding="10"/>

                    <TextBlock Text="ФИО" FontWeight="SemiBold" Margin="0 10 0 5"/>
                    <TextBox Text="{Binding SelectedUser.FullName}" 
                             Height="35"
                             Padding="10"/>

                    <TextBlock Text="Роль" FontWeight="SemiBold" Margin="0 10 0 5"/>
                    <ComboBox ItemsSource="{Binding Roles}"
                              SelectedItem="{Binding SelectedUser.Role}"
                              Height="35"
                              Padding="5"/>
                </StackPanel>

                <!-- Кнопки действия -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Сохранить"
                            Width="100"
                            Height="35"
                            Style="{StaticResource SaveButtonStyle}"
                            Command="{Binding SaveCommand}"/>

                    <Button Content="Отмена"
                            Width="100"
                            Height="35"
                            Style="{StaticResource CancelButtonStyle}"
                            Margin="10 0 0 0"
                            Command="{Binding CloseCommand}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>